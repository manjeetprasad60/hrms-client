/**
 * Firebase Client Configuration Adapter
 *
 * Re-exports the centralized Firebase configuration from src/config/firebase.ts.
 */

export * from '../../config/firebase';
export type { FirebaseWebConfig as FirebaseOptions } from '../../config/firebase';
